# Regression tests

`test-patterns.js` contains Node.js assertions for the URL matching rules used by EdgeClose.

The validation workflow currently performs syntax and JSON checks. Browser-level timer, schedule, alarm, and iframe behavior should be exercised manually in Microsoft Edge before publishing the release package.
